{
  "name": "sanskar-monorepo",
  "version": "1.0.0",
  "scripts": {
    "install:all": "npm install --prefix backend --legacy-peer-deps && npm install --prefix frontend --legacy-peer-deps",
    "build:frontend": "cd frontend && npx expo export -p web",
    "build": "npm run install:all && npm run build:frontend",
    "start": "cd backend && npm start"
  }
}